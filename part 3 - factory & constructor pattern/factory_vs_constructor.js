/**
 * ! constructor pattern and factory pattern both are doing same work
 * * but constructor pattern is more understandable
 * * most of the developers use Constructor pattern
 * * constructor pattern is recommended in our code
 */
